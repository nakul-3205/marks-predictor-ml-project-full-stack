name: Ping Render App

on:
  schedule:
    - cron: '*/14 * * * *'  # Executes every 14 minutes
  workflow_dispatch:  # Allows manual triggering

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping your Render app
        run: |
          curl -I https://mlmarkspredictor.onrender.com
